{
  "name": "bookmarklets",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "find": "^0.3.0",
    "uglify-js": "^3.19.3"
  },
  "scripts": {
    "build": "mkdir -p ./out && node build.js",
    "clean": "rm -r ./out/*"
  }
}
